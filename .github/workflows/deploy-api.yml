name: Deploy API - Secure CI/CD

on:
  push:
    paths: ['microservice/deploy/**']

jobs:

  build-sign-scan:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - name: Fix script permissions
        run: bash common/setup.sh

      - name: Build Image
        run: cd microservice/deploy && bash ./docker/build.sh

      - name: Generate SBOM
        run: syft localhost/deploy-api:v1.0.0 -o cyclonedx-json > sbom-deploy.json

      - name: Scan with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: localhost/deploy-api:v1.0.0
          format: sarif
          output: trivy-deploy.sarif
          severity: CRITICAL,HIGH
          exit-code: 1

      - name: Sign with Cosign
        run: cosign sign --yes localhost/deploy-api:v1.0.0

      - name: Upload SARIF
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-deploy.sarif
