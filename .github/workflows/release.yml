name: Release
on:
  push:
    tags: ["v*"]
jobs:
  build-sign-publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "Container build/sign/publish wired up in Days 22-23."
