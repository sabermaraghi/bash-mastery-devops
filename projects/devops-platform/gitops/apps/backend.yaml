# Leaf Application — ArgoCD syncs the backend k8s manifests into the cluster.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: backend
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/ericvalijani/bash-mastery-devops.git
    targetRevision: HEAD
    path: projects/devops-platform/services/backend/k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: backend
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
