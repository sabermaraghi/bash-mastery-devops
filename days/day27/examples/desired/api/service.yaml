apiVersion: v1
kind: Service
metadata:
  name: api
  labels:
    app.kubernetes.io/name: api
    app.kubernetes.io/managed-by: day27-argocd
spec:
  selector:
    app: api
  ports:
    - name: http
      port: 80
      targetPort: 80
