slack:
  webhook_url: "https://hooks.slack.com/services/PLACEHOLDER"
  # In production: injected via --set or sealed-secrets

grafana:
  dashboards:
    default:
      finops-cost:
        json: |
          <inline the entire JSON above or mount via ConfigMap>
